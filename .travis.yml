language: python
python:
  - "2.7"
  - "3.6"
branches:
  # Restrict push builds to only master
  only:
  - master
before_install:
  - set -e
install:
  - pip install -r requirements.txt
script:
  - make tests
